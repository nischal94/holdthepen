{
  "$comment": "Chrome expectedCall eval format. Validated by evals/evals.test.ts: every functionName must be a registered tool and every argument key must exist in that tool's inputSchema.",
  "tools": ["get_demo_status"],
  "cases": [
    {
      "id": "preflight-direct",
      "messages": [
        { "role": "user", "content": "What does the demo status tool on this page say?" }
      ],
      "expectedCall": [{ "functionName": "get_demo_status", "arguments": {} }]
    },
    {
      "id": "preflight-ambiguous",
      "messages": [
        { "role": "user", "content": "Is WebMCP working here?" }
      ],
      "expectedCall": [{ "functionName": "get_demo_status", "arguments": {} }]
    }
  ]
}
